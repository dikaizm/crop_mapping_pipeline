{
  "_comment": "Per-arch HP grid for train_segmentation.py --hp-grid. Top-level keys = arch names (match ARCH_CFG). Each arch: 'grid' (cartesian product) or 'combos' (explicit list). Keys: lr, weight_decay, warmup_epochs, sched_power, scheduler ('polynomial'|'cosine'), optimizer ('adamw'|'adam'|'sgd'), momentum (sgd only), grad_clip (0=off), batch_size. lr/weight_decay override ARCH_CFG defaults; others override config defaults. For a single shared space across all archs, use top-level {'grid':{...}} or {'combos':[...]}.",
  "deeplabv3plus_cbam": {
    "combos": [
      {"optimizer": "adamw", "lr": 1e-4, "weight_decay": 1e-4, "scheduler": "polynomial", "warmup_epochs": 0, "sched_power": 0.9, "batch_size": 8},
      {"optimizer": "adamw", "lr": 5e-5, "weight_decay": 1e-3, "scheduler": "polynomial", "warmup_epochs": 0, "sched_power": 0.9, "batch_size": 8},
      {"optimizer": "sgd", "lr": 1e-2, "weight_decay": 1e-4, "momentum": 0.9, "scheduler": "polynomial", "warmup_epochs": 0, "sched_power": 0.9, "batch_size": 8},
      {"optimizer": "sgd", "lr": 5e-3, "weight_decay": 5e-4, "momentum": 0.9, "scheduler": "cosine", "warmup_epochs": 3, "batch_size": 8}
    ]
  },
  "segformer": {
    "combos": [
      {"lr": 6e-5, "weight_decay": 1e-2, "optimizer": "adamw", "scheduler": "cosine", "warmup_epochs": 5,  "grad_clip": 1.0, "batch_size": 8},
      {"lr": 6e-5, "weight_decay": 5e-2, "optimizer": "adamw", "scheduler": "polynomial", "warmup_epochs": 5, "sched_power": 1.0, "grad_clip": 1.0, "batch_size": 8},
      {"lr": 3e-5, "weight_decay": 1e-2, "optimizer": "adamw", "scheduler": "cosine", "warmup_epochs": 10, "grad_clip": 1.0, "batch_size": 4},
      {"lr": 1e-3, "weight_decay": 1e-4, "optimizer": "sgd", "momentum": 0.9, "scheduler": "cosine", "warmup_epochs": 10, "grad_clip": 1.0, "batch_size": 8},
      {"lr": 5e-4, "weight_decay": 5e-4, "optimizer": "sgd", "momentum": 0.9, "scheduler": "polynomial", "warmup_epochs": 10, "sched_power": 0.9, "grad_clip": 1.0, "batch_size": 8}
    ]
  }
}
